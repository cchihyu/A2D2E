# experiments — Reproducibility Scripts

Each subdirectory corresponds to one paper result and is self-contained (except for shared dependencies in `src/` and `model_params/`).

| Directory | Paper section | Language |
|-----------|--------------|----------|
| `main_comparison/` | Table 2: ORMSE across methods and environments | Python |
| `gp_sensitivity/` | Figure 3: GPR length-scale sensitivity | Python |
| `wall_clock/` | Appendix C: wall-clock complexity | Python |
| `nonadditive/` | Appendix D.2: non-additive benchmarks | R |

See the README in each subdirectory for detailed instructions.
